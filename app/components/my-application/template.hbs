{{!-- <nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
        <li>{{#link-to "users"}}Users{{/link-to}}</li>
        {{/if}}
        <li>{{#link-to 'trips'}}Trips{{/link-to}}</li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li>{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
        <li>{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav> --}}

{{!-- <div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div> --}}

<div class="outer-container">
  {{outlet}}
</div>

<div class="footer-button-container auth-button-container">
    <div class="home-button">
      <button class="subtle-button home-button" {{action "home"}}><h4><span class="ion-ios-home"></span></h4></button>
    </div>
  {{#if isAuthenticated}}
    <div>
      <button class="subtle-button sign-out-button" {{action "signOut"}}>sign out</button>
    </div>
  {{else}}
    {{sign-in-modal signIn="signIn"}}
    {{sign-up-modal signUp="signUp"}}
  {{/if}}
</div>



{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}
